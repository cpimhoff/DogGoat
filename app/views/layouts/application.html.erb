<!DOCTYPE html>
<html>
<head>
  <title>DogGoat</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="site_header">
  <div id="hero">
    <%= link_to posts_path do %>
      <span id="dog">DOG</span><span id="goat">GOAT</span>
    <% end %>
  </div>
  <div id="navigation_bar">
    <div class='left lateral_list'>
      <span><%= link_to_unless_current "hot", posts_path(:sort_by => "hot")%></span>
      <span><%= link_to_unless_current "new", posts_path(:sort_by => "recent")%></span>
      <span><%= link_to_unless_current "featured", posts_path(:sort_by => "featured")%></span>
    </div>
    <div class='right lateral_list'>
      <% if session['member_id'].blank? %>
        <span><%= link_to_unless_current "login", controller: "login", action: "index" %></span>
      <% else %>
        <%= session['member_first_name'] %>
        <span><%= link_to_unless_current "upload", new_post_path%></span>
        <span><%= link_to_unless_current "logout", controller: "login", action: "logout" %></span>
      <% end %>
    </div>
  </div>
</div>

<% unless flash['msg'].blank? %>
  <div class="flash_block">
    <div class="msg">
      <%= flash['msg'] %>
    </div>
  </div>
<% end %>

<div class="main_content">
  <%= yield %>
</div>

<div id="site_footer">
  <div class="left lateral_list">
    <span><a href="https://github.com/cpimhoff/DogGoat">pre-alpha</a></span>
  </div>
  <div class='right lateral_list'>
    <span class="inactive">contact</span>
    <span class="inactive">policy</span>
    <span class="inactive">about</span>
  </div>
</div>

</body>
</html>
